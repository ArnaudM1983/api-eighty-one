{# templates/emails/order_shipped.html.twig #}
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: 'Helvetica', Arial, sans-serif; color: #333; line-height: 1.5; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; border: 1px solid #f0f0f0; }
        .header { text-align: center; border-bottom: 2px solid #000; padding-bottom: 20px; margin-bottom: 20px; }
        
        /* Style spécifique pour le badge d'expédition */
        .shipping-badge { 
            display: inline-block; 
            background: #000; 
            color: #fff; 
            padding: 5px 15px; 
            font-size: 11px; 
            font-weight: bold; 
            text-transform: uppercase; 
            margin-bottom: 10px;
        }

        .table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        .table th { text-align: left; background: #f8f8f8; padding: 10px; font-size: 11px; border-bottom: 1px solid #eee; }
        .table td { padding: 10px; border-bottom: 1px solid #eee; font-size: 14px; }
        
        .address-section { margin-top: 30px; display: table; width: 100%; }
        .address-column { display: table-cell; width: 50%; padding: 15px; background: #f9f9f9; border: 1px solid #eee; vertical-align: top; }
        .address-header { font-weight: bold; text-transform: uppercase; font-size: 12px; margin-bottom: 10px; display: block; border-bottom: 1px solid #ccc; }
        
        .button-wrapper { text-align: center; margin: 30px 0; }
        .button { background: #01B0F0; color: #fff; padding: 15px 25px; text-decoration: none; font-weight: bold; border-radius: 4px; text-transform: uppercase; font-size: 13px; }

        .footer { text-align: center; font-size: 11px; color: #999; margin-top: 40px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="shipping-badge">Commande Expédiée</div>
            <h1 style="margin:0;">EIGHTY ONE STORE</h1>
            <p style="text-transform: uppercase; font-size: 12px; letter-spacing: 1px;">Votre colis est en route ! (Commande #{{ order.id }})</p>
        </div>

        <p>Bonjour {{ order.shippingInfo.firstName }},</p>
        <p>Bonne nouvelle ! Votre commande a été préparée avec soin et vient d'être remise à notre transporteur.</p>

        <table class="table">
            <thead>
                <tr>
                    <th>Articles expédiés</th>
                    <th style="text-align: center;">Qté</th>
                </tr>
            </thead>
            <tbody>
                {% for item in order.items %}
                    <tr>
                        <td>
                            <strong>{{ item.product.name }}</strong>
                            {% if item.variant %}
                                <br><small style="color: #666;">{{ item.variant.name }}</small>
                            {% endif %}
                        </td>
                        <td style="text-align: center;">{{ item.quantity }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <div class="address-section">
            <div class="address-column">
                <span class="address-header">Destinataire</span>
                {{ order.shippingInfo.firstName }} {{ order.shippingInfo.lastName }}<br>
                {{ order.shippingInfo.address }}<br>
                {{ order.shippingInfo.phone }}<br>
                {{ order.shippingInfo.postalCode }} {{ order.shippingInfo.city }}<br>
                {{ order.shippingInfo.country }}
            </div>

            <div class="address-column" style="border-left: 10px solid white;">
                <span class="address-header">Lieu de livraison</span>
                {% if order.shippingInfo.pudoId %}
                    <strong>Point Relais : {{ order.shippingInfo.pudoName }}</strong><br>
                    {{ order.shippingInfo.pudoAddress }}<br>
                    {{ order.shippingInfo.pudoPostalCode }} {{ order.shippingInfo.pudoCity }}
                {% else %}
                    <strong>À domicile</strong><br>
                    {{ order.shippingInfo.address }}<br>
                    {{ order.shippingInfo.postalCode }} {{ order.shippingInfo.city }}
                {% endif %}
            </div>
        </div>

        <div class="button-wrapper">
            <a href="https://eightyone.fr/account/orders" class="button">Suivre ma commande</a>
        </div>

        <p style="font-size: 13px; color: #666; text-align: center;">
            Note : Il peut s'écouler jusqu'à 24h avant que le lien de suivi ne devienne actif sur le site du transporteur.
        </p>

        <div class="footer">
            Eighty One Store - 2026<br>
            Ceci est un message automatique, merci de ne pas y répondre.
        </div>
    </div>
</body>
</html>